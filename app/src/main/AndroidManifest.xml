<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.timothyodonga.mentalmath">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainMenu" />
        <activity android:name=".DivideMenu" />
        <activity android:name=".DivideTutorial" />
        <activity android:name=".MultiplyMenu" />
        <activity android:name=".MultiplyTutorial" />
        <activity android:name=".DividePractice" />
        <activity android:name=".MultiplyPractice" />
        <activity android:name=".SquaringMenu" />
        <activity android:name=".SquaringTutorial" />
        <activity android:name=".SquaringPractice"></activity>
    </application>

</manifest>